# PHP 8.2 升级实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将最低 PHP 版本从 8.0 升级到 8.2，并删除所有废弃代码

**Architecture:** 删除废弃函数和类，重命名 MchTransfer 为 Transfer，更新所有命名空间引用

**Tech Stack:** PHP 8.2+, Composer, PHPUnit, PHPStan

---

## 文件结构变更概览

### 删除的文件
```
src/Functions.php (删除 3 个函数)
src/Plugin/Wechat/StartPlugin.php
src/Plugin/Wechat/V3/Marketing/Transfer/ (整个目录，10 个文件)
tests/Plugin/Wechat/StartPluginTest.php
tests/Plugin/Wechat/V3/Marketing/Transfer/ (整个目录，10 个文件)
```

### 重命名的目录
```
src/Plugin/Wechat/V3/Marketing/MchTransfer/ → src/Plugin/Wechat/V3/Marketing/Transfer/
tests/Plugin/Wechat/V3/Marketing/MchTransfer/ → tests/Plugin/Wechat/V3/Marketing/Transfer/
```

### 修改的文件
```
composer.json
src/Shortcut/Wechat/TransferShortcut.php
src/Shortcut/Wechat/QueryShortcut.php
src/Shortcut/Wechat/CancelShortcut.php
tests/Shortcut/Wechat/TransferShortcutTest.php
tests/Shortcut/Wechat/QueryShortcutTest.php
tests/Shortcut/Wechat/CancelShortcutTest.php
web/docs/v3/wechat/all.md
CHANGELOG.md
```

---

## Task 1: 更新 composer.json 最低 PHP 版本

**Files:**
- Modify: `composer.json:19`

- [ ] **Step 1: 修改 PHP 版本要求**

将第 19 行从：
```json
"php": ">=8.0",
```
改为：
```json
"php": ">=8.2",
```

- [ ] **Step 2: 验证 composer.json 语法**

Run: `composer validate`
Expected: `./composer.json is valid`

---

## Task 2: 删除废弃函数 get_alipay_config

**Files:**
- Modify: `src/Functions.php:87-97`

- [ ] **Step 1: 删除 get_alipay_config 函数**

删除第 87-97 行（包括 PHPDoc 和 `#[Deprecated]` 注解）：

```php
/**
 * @throws ContainerException
 * @throws ServiceNotFoundException
 */
#[Deprecated(reason: '自 v3.7.5 开始废弃', replacement: 'get_provider_config')]
function get_alipay_config(array $params = []): array
{
    $alipay = Pay::get(ConfigInterface::class)->get('alipay');

    return $alipay[get_tenant($params)] ?? [];
}
```

删除后，第 86 行（空行）与第 98 行（`function get_alipay_url`）之间应无其他内容。

---

## Task 3: 删除废弃函数 get_wechat_config

**Files:**
- Modify: `src/Functions.php:138-148`

- [ ] **Step 1: 删除 get_wechat_config 函数**

删除第 138-148 行（包括 PHPDoc 和 `#[Deprecated]` 注解）：

```php
/**
 * @throws ContainerException
 * @throws ServiceNotFoundException
 */
#[Deprecated(reason: '自 v3.7.5 开始废弃', replacement: 'get_provider_config')]
function get_wechat_config(array $params = []): array
{
    $wechat = Pay::get(ConfigInterface::class)->get('wechat');

    return $wechat[get_tenant($params)] ?? [];
}
```

---

## Task 4: 删除废弃函数 get_unipay_config

**Files:**
- Modify: `src/Functions.php:488-498`

- [ ] **Step 1: 删除 get_unipay_config 函数**

删除第 488-498 行（包括 PHPDoc 和 `#[Deprecated]` 注解）：

```php
/**
 * @throws ContainerException
 * @throws ServiceNotFoundException
 */
#[Deprecated(reason: '自 v3.7.5 开始废弃', replacement: 'get_provider_config')]
function get_unipay_config(array $params = []): array
{
    $unipay = Pay::get(ConfigInterface::class)->get('unipay');

    return $unipay[get_tenant($params)] ?? [];
}
```

---

## Task 5: 删除 JetBrains PhpStorm Deprecated import

**Files:**
- Modify: `src/Functions.php:7`

- [ ] **Step 1: 删除 Deprecated import**

删除第 7 行：
```php
use JetBrains\PhpStorm\Deprecated;
```

---

## Task 6: 删除废弃的 Wechat StartPlugin

**Files:**
- Delete: `src/Plugin/Wechat/StartPlugin.php`

- [ ] **Step 1: 删除文件**

Run: `rm src/Plugin/Wechat/StartPlugin.php`

---

## Task 7: 删除废弃的 Transfer 插件目录

**Files:**
- Delete: `src/Plugin/Wechat/V3/Marketing/Transfer/` (整个目录)

- [ ] **Step 1: 删除整个 Transfer 目录**

Run: `rm -rf src/Plugin/Wechat/V3/Marketing/Transfer/`

此操作将删除以下 10 个文件：
- `CreatePlugin.php`
- `Batch/QueryByWxPlugin.php`
- `Batch/QueryPlugin.php`
- `Detail/QueryByWxPlugin.php`
- `Detail/QueryPlugin.php`
- `Receipt/CreatePlugin.php`
- `Receipt/QueryPlugin.php`
- `ReceiptDetail/CreatePlugin.php`
- `ReceiptDetail/QueryPlugin.php`
- `DownloadReceiptPlugin.php`

---

## Task 8: 重命名 MchTransfer 目录为 Transfer（源码）

**Files:**
- Rename: `src/Plugin/Wechat/V3/Marketing/MchTransfer/` → `src/Plugin/Wechat/V3/Marketing/Transfer/`

- [ ] **Step 1: 重命名目录**

Run: `mv src/Plugin/Wechat/V3/Marketing/MchTransfer src/Plugin/Wechat/V3/Marketing/Transfer`

---

## Task 9: 更新 Transfer 插件命名空间（CreatePlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/CreatePlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将第 5 行从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将第 40 行从：
```php
Logger::debug('[Wechat][Marketing][MchTransfer][CreatePlugin] 插件开始装载', ['rocket' => $rocket]);
```
改为：
```php
Logger::debug('[Wechat][Marketing][Transfer][CreatePlugin] 插件开始装载', ['rocket' => $rocket]);
```

将第 64 行从：
```php
Logger::info('[Wechat][Marketing][MchTransfer][CreatePlugin] 插件装载完毕', ['rocket' => $rocket]);
```
改为：
```php
Logger::info('[Wechat][Marketing][Transfer][CreatePlugin] 插件装载完毕', ['rocket' => $rocket]);
```

---

## Task 10: 更新 Transfer 插件命名空间（QueryPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/QueryPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 11: 更新 Transfer 插件命名空间（QueryByWxPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/QueryByWxPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 12: 更新 Transfer 插件命名空间（CancelPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/CancelPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 13: 更新 Transfer 插件命名空间（InvokeJsapiPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/InvokeJsapiPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 14: 更新 Transfer 插件命名空间（InvokeAndroidPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/InvokeAndroidPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 15: 更新 Transfer 插件命名空间（InvokeIosPlugin）

**Files:**
- Modify: `src/Plugin/Wechat/V3/Marketing/Transfer/InvokeIosPlugin.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer;
```

- [ ] **Step 2: 更新日志消息中的命名空间**

将日志消息中的 `[MchTransfer]` 改为 `[Transfer]`

---

## Task 16: 更新 TransferShortcut 命名空间引用

**Files:**
- Modify: `src/Shortcut/Wechat/TransferShortcut.php`

- [ ] **Step 1: 删除旧的 MchTransfer import**

删除第 16 行：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\CreatePlugin;
```

- [ ] **Step 2: 添加新的 Transfer import**

在第 16 行位置添加：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CreatePlugin;
```

- [ ] **Step 3: 删除 transferPlugins 方法中对已删除类的引用**

删除第 41-53 行的 `transferPlugins()` 方法：
```php
public function transferPlugins(): array
{
    return [
        StartPlugin::class,
        \Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CreatePlugin::class,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ];
}
```

- [ ] **Step 4: 将 mchTransferPlugins 改名为 transferPlugins**

将第 55-67 行的 `mchTransferPlugins()` 方法名改为 `transferPlugins()`：
```php
public function transferPlugins(): array
{
    return [
        StartPlugin::class,
        CreatePlugin::class,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ];
}
```

- [ ] **Step 5: 更新 defaultPlugins 方法**

由于原 `transferPlugins()` 已删除，`defaultPlugins()` 应调用新的 `transferPlugins()`：
```php
public function defaultPlugins(): array
{
    return $this->transferPlugins();
}
```

---

## Task 17: 更新 QueryShortcut 命名空间引用

**Files:**
- Modify: `src/Shortcut/Wechat/QueryShortcut.php`

- [ ] **Step 1: 更新 MchTransfer imports**

将第 16-18 行从：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\QueryByWxPlugin as MchTransferQueryByWxPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\QueryPlugin as MchTransferQueryPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\Detail\QueryPlugin as TransferQueryPlugin;
```
改为：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\QueryByWxPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\QueryPlugin;
```

注意：删除了 `Transfer\Detail\QueryPlugin` 引用，因为该类已被删除。

- [ ] **Step 2: 更新 mchTransferPlugins 方法为 transferPlugins**

将第 143-161 行的 `mchTransferPlugins()` 方法改为：
```php
protected function transferPlugins(array $params): array
{
    $query = QueryPlugin::class;

    if (isset($params['transfer_bill_no'])) {
        $query = QueryByWxPlugin::class;
    }

    return [
        StartPlugin::class,
        $query,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ];
}
```

- [ ] **Step 3: 删除旧的 transferPlugins 方法**

删除第 252-264 行的旧 `transferPlugins()` 方法（引用已删除的 `TransferQueryPlugin`）。

---

## Task 18: 更新 CancelShortcut 命名空间引用

**Files:**
- Modify: `src/Shortcut/Wechat/CancelShortcut.php`

- [ ] **Step 1: 更新 import**

将第 16 行从：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\CancelPlugin;
```
改为：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CancelPlugin;
```

- [ ] **Step 2: 将 mchTransferPlugins 改名为 transferPlugins**

将第 41-53 行的 `mchTransferPlugins()` 方法名改为 `transferPlugins()`

---

## Task 19: 删除测试文件（Wechat StartPlugin）

**Files:**
- Delete: `tests/Plugin/Wechat/StartPluginTest.php`

- [ ] **Step 1: 删除测试文件**

Run: `rm tests/Plugin/Wechat/StartPluginTest.php`

---

## Task 20: 删除测试文件（Transfer 目录）

**Files:**
- Delete: `tests/Plugin/Wechat/V3/Marketing/Transfer/` (整个目录)

- [ ] **Step 1: 删除整个 Transfer 测试目录**

Run: `rm -rf tests/Plugin/Wechat/V3/Marketing/Transfer/`

此操作将删除 10 个测试文件。

---

## Task 21: 重命名测试目录 MchTransfer → Transfer

**Files:**
- Rename: `tests/Plugin/Wechat/V3/Marketing/MchTransfer/` → `tests/Plugin/Wechat/V3/Marketing/Transfer/`

- [ ] **Step 1: 重命名测试目录**

Run: `mv tests/Plugin/Wechat/V3/Marketing/MchTransfer tests/Plugin/Wechat/V3/Marketing/Transfer`

---

## Task 22: 更新测试文件命名空间（CreatePluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/CreatePluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从：
```php
namespace Yansongda\Pay\Tests\Plugin\Wechat\V3\Marketing\MchTransfer;
```
改为：
```php
namespace Yansongda\Pay\Tests\Plugin\Wechat\V3\Marketing\Transfer;
```

---

## Task 23: 更新测试文件命名空间（QueryPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/QueryPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 24: 更新测试文件命名空间（QueryByWxPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/QueryByWxPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 25: 更新测试文件命名空间（CancelPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/CancelPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 26: 更新测试文件命名空间（InvokeJsapiPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/InvokeJsapiPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 27: 更新测试文件命名空间（InvokeAndroidPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/InvokeAndroidPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 28: 更新测试文件命名空间（InvokeIosPluginTest）

**Files:**
- Modify: `tests/Plugin/Wechat/V3/Marketing/Transfer/InvokeIosPluginTest.php`

- [ ] **Step 1: 更新命名空间声明**

将命名空间从 `MchTransfer` 改为 `Transfer`

---

## Task 29: 更新 TransferShortcutTest 命名空间引用

**Files:**
- Modify: `tests/Shortcut/Wechat/TransferShortcutTest.php`

- [ ] **Step 1: 更新 import**

将第 15 行从：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\CreatePlugin;
```
改为：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CreatePlugin;
```

- [ ] **Step 2: 删除 testDefault 测试方法**

删除 `testDefault()` 方法，因为旧的 `Transfer\CreatePlugin` 已删除，新行为与 `testMch` 相同。

- [ ] **Step 3: 更新 testMch 为 testDefault**

将 `testMch()` 方法改名为 `testDefault()`，并更新期望值：
```php
public function testDefault(): void
{
    self::assertEquals([
        StartPlugin::class,
        CreatePlugin::class,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ], $this->plugin->getPlugins([]));
}
```

---

## Task 30: 更新 QueryShortcutTest 命名空间引用

**Files:**
- Modify: `tests/Shortcut/Wechat/QueryShortcutTest.php`

- [ ] **Step 1: 更新 imports**

将第 15-17 行从：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\QueryByWxPlugin as MchTransferQueryByWxPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\QueryPlugin as MchTransferQueryPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\Detail\QueryPlugin as TransferQueryPlugin;
```
改为：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\QueryByWxPlugin;
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\QueryPlugin;
```

- [ ] **Step 2: 删除 testTransfer 测试方法**

删除 `testTransfer()` 方法，因为旧 Transfer 插件已删除。

- [ ] **Step 3: 更新 testMchTransfer 为 testTransfer**

将 `testMchTransfer()` 方法改名为 `testTransfer()`，并更新类引用：
```php
public function testTransfer(): void
{
    self::assertEquals([
        StartPlugin::class,
        QueryPlugin::class,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ], $this->plugin->getPlugins(['_action' => 'transfer']));

    self::assertEquals([
        StartPlugin::class,
        QueryByWxPlugin::class,
        AddPayloadBodyPlugin::class,
        AddPayloadSignaturePlugin::class,
        AddRadarPlugin::class,
        VerifySignaturePlugin::class,
        ResponsePlugin::class,
        ParserPlugin::class,
    ], $this->plugin->getPlugins(['_action' => 'transfer', 'transfer_bill_no' => '123']));
}
```

---

## Task 31: 更新 CancelShortcutTest 命名空间引用

**Files:**
- Modify: `tests/Shortcut/Wechat/CancelShortcutTest.php`

- [ ] **Step 1: 更新 import**

将第 16 行从：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\CancelPlugin;
```
改为：
```php
use Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CancelPlugin;
```

- [ ] **Step 2: 删除 testMchTransfer 测试方法**

删除 `testMchTransfer()` 方法，因为它与 `testDefault()` 行为相同。

---

## Task 32: 删除文档中废弃的章节

**Files:**
- Modify: `web/docs/v3/wechat/all.md:365-414`

- [ ] **Step 1: 删除「商家转账到零钱」章节**

删除第 365-414 行（整个章节）：
```markdown
### 商家转账到零钱

- 发起商家转账

  `\Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\CreatePlugin`

... (整个章节内容)
```

---

## Task 33: 更新 CHANGELOG

**Files:**
- Modify: `CHANGELOG.md`

- [ ] **Step 1: 添加新版本变更记录**

在文件顶部添加新的版本记录：

```markdown
## v3.8.0 - 2026-04-10

### BREAKING CHANGES

- change: 最低 PHP 版本要求从 8.0 升级到 8.2
- delete: 移除废弃函数 `get_alipay_config`、`get_wechat_config`、`get_unipay_config`，请使用 `get_provider_config` 代替
- delete: 移除废弃的 `Wechat\StartPlugin`，请使用 `Yansongda\Artful\Plugin\StartPlugin` 代替
- delete: 移除废弃的微信 Transfer 插件（因微信支付 API 变更）
- change: 重命名 `MchTransfer` 命名空间为 `Transfer`

### Removed

- `src/Plugin/Wechat/StartPlugin.php`
- `src/Plugin/Wechat/V3/Marketing/Transfer/` 目录
- `get_alipay_config()` 函数
- `get_wechat_config()` 函数
- `get_unipay_config()` 函数

### Changed

- `Yansongda\Pay\Plugin\Wechat\V3\Marketing\MchTransfer\*` → `Yansongda\Pay\Plugin\Wechat\V3\Marketing\Transfer\*`
```

---

## Task 34: 运行测试验证

- [ ] **Step 1: 运行测试**

Run: `composer test`
Expected: 所有测试通过

- [ ] **Step 2: 运行静态分析**

Run: `composer analyse`
Expected: 无错误

- [ ] **Step 3: 运行代码风格检查**

Run: `composer cs-fix`
Expected: 无输出（代码风格正确）

---

## Task 35: 提交变更

- [ ] **Step 1: 查看变更状态**

Run: `git status`

- [ ] **Step 2: 添加所有变更**

Run: `git add .`

- [ ] **Step 3: 提交变更**

Run:
```bash
git commit -m "feat: 升级 PHP 最低版本到 8.2 并移除废弃代码

BREAKING CHANGES:
- 最低 PHP 版本要求从 8.0 升级到 8.2
- 删除废弃函数 get_alipay_config, get_wechat_config, get_unipay_config
- 删除废弃的 Wechat\StartPlugin 和 Transfer 插件
- 重命名 MchTransfer 命名空间为 Transfer"
```

---

## 自检清单

| 检查项 | 状态 |
|--------|------|
| Spec 覆盖率 | ✅ 所有设计文档中的变更都有对应任务 |
| Placeholder 扫描 | ✅ 无 TBD/TODO |
| 类型一致性 | ✅ 命名空间变更在所有文件中保持一致 |
| 测试覆盖 | ✅ 删除源码时同步删除测试 |